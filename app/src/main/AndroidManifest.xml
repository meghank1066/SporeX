<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SPOREX_App">

        <activity android:name=".useraccount.RegisterActivity" />
        <activity android:name=".ui.components.UploadActivity" />
        <activity android:name=".ui.components.ConfirmationActivity" />
        <activity android:name=".ui.components.ResultActivity" />
        <activity android:name=".useraccount.ProfileActivity" />
        <activity android:name=".useraccount.UserSettings" />
<!--        <activity android:name=".useraccount.LoginActivity" />-->
        <activity android:name=".ui.components.ProductsActivity" />
        <activity android:name=".ui.components.ProductDetailActivity" />
        <activity android:name=".ui.community.MyPostsActivity" />
        <activity android:name=".ui.community.CommunityHP" />
        <activity android:name=".ui.community.AsthmaSociety" />
        <activity android:name=".ui.community.CreatePostActivity" />
        <activity android:name=".useraccount.EditProfileActivity" />
        <activity android:name=".useraccount.YourAccountActivity" />
        <activity android:name=".useraccount.EditPasswordActivity" />
        <activity android:name=".ui.alerts.NotificationsActivity" />
        <activity android:name=".ui.components.HistoryActivity" />
        <activity android:name=".ui.device.DeviceActivity" />

<activity android:name=".MainActivity" />



        <!--            android:name=".useraccount.LoginActivity"-->

        <activity
            android:name=".useraccount.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.SPOREX_App">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

</manifest>
